#!/bin/bash

# Ejercicio 3 - Comprobación de parámetros y gestión de archivos
# ---------------------------------------------------------------
# 1. Verificar si se ha pasado un parámetro obligatorio (nombre de fichero).
#    - Si no se ha pasado, mostrar la sintaxis correcta y finalizar el script
#      (opcionalmente se puede solicitar el nombre por teclado).
#
# 2. Si el fichero existe:
#    - Mostrar su contenido en pantalla.
#
# 3. Si el fichero NO existe:
#    - Crear el fichero con contenido por defecto (de varias líneas)
#      utilizando un heredoc (<<EOF).
